import {

}
from '../actions'

import 'babel-polyfill'

export default function exampleState(state = initialState, action) {
    switch (action.type) {
    default:
        return state
    }
}

const initialState = {}
