import React from 'react'
import { TableCell } from '../Table'

const NameColCell = props => (
    <TableCell style={{ padding: '0 1em', justifyContent: 'flex-start', minWidth: '300px' }} {...props}></TableCell>
)

export default NameColCell
