import { shallowMount } from '@vue/test-utils'
import Header from '@/components/Header.vue'

describe('Header.vue', () => {
  it('everything is setup correctly', () => {
    
    expect(true).toBe(true)
  })
})
