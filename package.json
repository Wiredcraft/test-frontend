{
  "name": "wiredcraft-masonry-layout",
  "private": true,
  "scripts": {
    "dev": "npx lerna run dev",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.2.1"
  }
}
